{
  "name": "diet-app",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm --prefix backend run start:dev\" \"npm --prefix frontend run start\"",
    "test": "npm --prefix backend run test:e2e && npm --prefix frontend run test:ci",
    "e2e": "cypress run",
    "e2e:open": "cypress open",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "format": "npm --prefix backend run format && npm --prefix frontend run format"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cypress": "^14.1.0"
  }
}
